\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % vide tous les en-têtes et pieds par défaut

\fancyfoot[LE]{\thepage} % Left sur pages Even (paires = gauche)
\fancyfoot[RO]{\thepage} % Right sur pages Odd (impaires = droite)

\renewcommand{\headrulewidth}{0pt} % supprime la ligne d'en-tête

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[LE]{\thepage}
  \fancyfoot[RO]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

\usepackage{etoolbox}
\pretocmd{\tableofcontents}{\newpage}{}{}

\raggedbottom % Empêche d'étirer verticalement les paragraphes pour remplir les pages

\usepackage{fontspec}
\setmainfont{Georgia}[
  FakeBold=0.3
]

% Quotes françaises
\usepackage{etoolbox}
\renewenvironment{quote}{
  \itshape«~
}{
  ~»
}


% Tableaux
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{booktabs}
\usepackage{etoolbox}

% Couleur des lignes alternées
\definecolor{rowgray}{gray}{0.93}

% Style automatique pour tous les tableaux
\AtBeginEnvironment{longtable}{
  \rowcolors{2}{rowgray}{white}
}